services:
  app:
    image: node:20
    buildCommand: cd Back && npm install && cd ../Front && npm install && npm run build
    runCommand: cd Back && npm start
    environment:
      - NODE_ENV=production
    port: 3000
