import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  palette: {
    primary: {
      main: "#0398ae", // Cambia esto al color que quieras
    },
  },
});

export default theme;
