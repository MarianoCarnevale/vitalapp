import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  palette: {
    primary: {
      main: "#0398ae",
    },
    white: {
      main: "#ffffff",
    },
    secondary: {
      main: "#878787",
    },
  },
});

export default theme;
